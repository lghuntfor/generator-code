package ${packageName}.manager;

import com.ndccloud.commons.mybatis.plus.manager.BaseManager;
import ${packageName}.domain.${data.className};

/**
 * ${data.tableComment}通用业务接口
 * \@author ${author}
 * \@date ${date(),"yyyy-MM-dd HH:mm:ss"}
 * \@version ${version}
 */
public interface ${data.className}Manager extends BaseManager<${data.className}> {

}
